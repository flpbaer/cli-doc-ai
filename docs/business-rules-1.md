# flpbaer/cli-doc-ai — Business Rules

> Generated by cli-doc-ai on 2026-07-01

# Regras de Negócio Extraídas do Código Base

## Domínio: Interação com IA (src/core/ai.ts, src/core/openrouter.ts)

- **Regra: Fallback de modelos em caso de limite de taxa (429)**  
  - **Onde é enforceado**: `src/core/openrouter.ts` (função `callOpenRouter`)  
  - **Por que existe**: Garante continuidade do serviço quando o modelo principal está sobrecarregado, tentando modelos alternativos gratuitos na fila de fallback.  
  - **Ambiente**: Apenas aplicável ao provedor OpenRouter.

- **Regra: Detecção automática de idioma na resposta da IA**  
  - **Onde é enforceado**: `src/core/ai.ts` (função `detectLanguage` e `generateInLanguage`)  
  - **Por que existe**: Garante que a saída da IA esteja em português do Brasil ou inglês, mesmo que o modelo não siga a instrução inicial.  
  - **Ambiente**: Aplicável a todos os prompts que exigem linguagem específica.

- **Regra: Limitação de tokens por resposta da IA**  
  - **Onde é enforceado**: `src/core/ai.ts` (parâmetro `maxTokens` nas opções de IA)  
  - **Por que existe**: Evita respostas excessivamente longas que possam exceder o contexto do modelo.  
  - **Ambiente**: Configurável por usuário ou padrão (2048 tokens).

---

## Domínio: Manipulação de Git (src/core/git.ts)

- **Regra: Ignorar arquivos específicos em diffs**  
  - **Onde é enforceado**: `src/core/git.ts` (variável `IGNORE` e `DIFF_EXTENSIONS`)  
  - **Por que existe**: Foca apenas em arquivos relevantes para documentação (código, configurações, markdown).  
  - **Ambiente**: Aplicável a todas as operações de coleta de contexto git.

- **Regra: Limitação de caracteres em diffs**  
  - **Onde é enforceado**: `src/core/git.ts` (constante `MAX_DIFF_CHARS`)  
  - **Por que existe**: Evita sobrecarregar a IA com diffs muito grandes.  
  - **Ambiente**: Aplicável ao coletar contexto de mudanças.

---

## Domínio: Geração de Documentação (src/core/changelog.ts, src/core/prompts.ts)

- **Regra: Estrutura fixa para entradas no CHANGELOG.md**  
  - **Onde é enforceado**: `src/core/changelog.ts` (função `renderEntry`)  
  - **Por que existe**: Garante consistência na formatação das entradas (versão, data, PR, resumo).  
  - **Ambiente**: Aplicável a todas as atualizações do CHANGELOG.

- **Regra: Linguagem obrigatória em prompts**  
  - **Onde é enforceado**: `src/core/prompts.ts` (funções `languageInstruction` e `languageReminder`)  
  - **Por que existe**: Garante que a documentação seja gerada em português do Brasil ou inglês, conforme solicitado.  
  - **Ambiente**: Aplicável a todos os prompts de geração de texto.

- **Regra: Limitação de caracteres no contexto da IA**  
  - **Onde é enforceado**: `src/core/analyzer.ts` (constantes `MAX_FILE_CHARS` e `MAX_TOTAL_CHARS`)  
  - **Por que existe**: Evita que o contexto exceda a capacidade de processamento da IA.  
  - **Ambiente**: Aplicável ao escanear o código-base.

---

## Domínio: Versão do Projeto (src/core/version.ts)

- **Regra: Detecção automática da versão do projeto**  
  - **Onde é enforceado**: `src/core/version.ts` (função `detectVersion`)  
  - **Por que existe**: Usa fontes confiáveis (package.json, git tags) para identificar a versão atual.  
  - **Ambiente**: Aplicável a todas as operações que exigem identificação de versão.

---

## Domínio: Validações de Entrada (src/cli.ts, src/action.ts)

- **Regra: API key obrigatória para OpenRouter**  
  - **Onde é enforceado**: `src/cli.ts` e `src/action.ts` (validação de `OPENROUTER_API_KEY`)  
  - **Por que existe**: Garante acesso à API do OpenRouter para geração de documentação.  
  - **Ambiente**: Aplicável ao modo OpenRouter.

- **Regra: Validação de repositório vazio**  
  - **Onde é enforceado**: `src/core/git.ts` (função `getRepoName`)  
  - **Por que existe**: Evita falhas ao não haver histórico de commits.  
  - **Ambiente**: Aplicável ao coletar contexto git.

---

## Domínio: Regras de Documentação (docs/business-rules.md)

- **Regra: Extração de regras de negócio por módulo**  
  - **Onde é enforceado**: `docs/business-rules.md` (gerado automaticamente)  
  - **Por que existe**: Fornece critérios claros para revisão de código com base em validações codificadas.  
  - **Ambiente**: Aplicável a todos os módulos do projeto.

- **Regra: Documentação de templates de implementação**  
  - **Onde é enforceado**: `docs/templates.md` (gerado automaticamente)  
  - **Por que existe**: Garante consistência em novas implementações seguindo padrões existentes.  
  - **Ambiente**: Aplicável a módulos com padrões de código repetitivos.

---

## ⚠ NEEDS CONFIRMATION

- **Regra: Tratamento de erros em prompts de IA**  
  - **Onde é enforceado**: `src/core/ai.ts` (tratamento de respostas vazias ou erros)  
  - **Por que existe**: Não está claro se a tradução automática de respostas incorretas é suficiente.  
  - **Ambiente**: Aplicável a todos os prompts.

- **Regra: Prioridade de arquivos em escaneamento**  
  - **Onde é enforceado**: `src/core/analyzer.ts` (variável `PRIORITY_FILES`)  
  - **Por que existe**: Não está documentado por que esses arquivos são priorizados.  
  - **Ambiente**: Aplicável ao escaneamento do código-base.

---

Essas regras foram extraídas com base em lógica de validação, cálculo e restrições explícitas no código. Regras ambíguas ou implícitas foram marcadas para validação humana.
